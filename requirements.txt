# Author-time-only Python dependencies.
#
# These packages are NEVER bundled into the .mez connector and are NEVER
# required to run the M-based test suite. They are used by:
#   - CI/Scripts/Generate-ArrowFixtures.py  — regenerates deterministic Arrow
#     IPC byte fixtures under tests/fixtures/ (author-time, no credentials).
#   - CI/Scripts/Probe-MultiEvaluate.py     — live baseline oracle for
#     multi-EVALUATE DAX queries; requires PBI credentials at run-time.
# Once Arrow fixtures are generated, the M-based parser tests parse the
# committed bytes via Binary.FromText(..., BinaryEncoding.Hex).
#
# Setup (one-time per developer machine):
#   python -m venv .venv
#   .\.venv\Scripts\python.exe -m pip install -r requirements.txt
#
# Pinned to the versions validated on 2026-06-30 / 2026-07-14.
pyarrow==24.0.0
msal==1.31.0
requests==2.32.3
